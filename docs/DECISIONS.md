# Decisions & Findings — v1.0.0 baseline audit

Findings from a full code review, logged here rather than silently fixed, so the baseline is honest about what v1.0.0 actually does. Numbered for reference from other docs. Status = current state, not yet remediated unless noted.

| #   | Finding                                                                                                              | Severity                          | Status                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Double softmax: `forward()` softmaxes, `predict.py` softmaxes again                                                  | Correctness (predict-prob values) | **Fixed** (session 2) — `classify_sentiment` no longer re-softmaxes; uses `forward()`'s output directly.                                                                                                                                                                                                                                                                              |
| 2   | `BCEWithLogitsLoss` used against softmax-normalized, mutually-exclusive output                                       | Correctness (training)            | Open — needs retrain, deferred to 2.0                                                                                                                                                                                                                                                                                                                                                 |
| 3   | Text preprocessed twice per `/predict` request (once explicit, once inside `classify_sentiment`)                     | Performance                       | **Fixed** (session 2) — `classify_sentiment` now takes already-cleaned text; controller preprocesses once and passes the cleaned string through. **Breaking internal API change**: param renamed `input_text`→`cleaned_text`, callers must preprocess before calling. `basic.py`/`gui.py` already called it this way; both verified compatible.                                       |
| 4   | Missing model checkpoint fails silently ("Model not loaded", no exception, server still starts)                      | Reliability                       | **Fixed** (session 2) — classifier tracks `model_loaded`; `classify_sentiment` raises `RuntimeError` rather than serving from randomly-initialized weights; `main.py` maps that to HTTP 503 via a global exception handler; `/health` reports real `model_loaded` state and returns 503 when false; `basic.py`/`gui.py` catch the error and exit/display cleanly instead of crashing. |
| 5   | `dev-requirements.txt` pins `torch==2.1.0+cu121`, breaks `pip install` off CUDA machines                             | Portability / DX                  | **Fixed** (session 2) — dropped `+cu121` suffix from `torch`, `torchaudio`, `torchvision` pins.                                                                                                                                                                                                                                                                                       |
| 6   | `BertTokenizer.from_pretrained()` reloaded on every `TextDataset` instantiation → every inference request            | Performance (latency)             | **Fixed** (session 2) — tokenizer loaded once via `lru_cache`d `get_tokenizer()`, cached on the classifier, reused across requests.                                                                                                                                                                                                                                                   |
| 7   | Model weights (`*.pth`) not committed, not fetched anywhere — fresh clone/Docker build cannot serve real predictions | Reliability / DX                  | **Known limitation, by design (gitignored on purpose — weights are large binaries)**. Needs an explicit fetch step (release asset / registry) before this stops being silent. Still open — decision, not code; carried into ROADMAP.md's new production track.                                                                                                                        |
| 8   | `Field(..., example=...)` is Pydantic v1 syntax under pinned `pydantic==2.4.2`                                       | Compat, unverified impact         | **Fixed** (session 2) — migrated all four models to `json_schema_extra={"example": ...}`; also fixed `TextProbResponse.sentiment_prob`'s example, which was a string instead of a list.                                                                                                                                                                                               |
| 9   | No CORS middleware                                                                                                   | Blocks browser frontend           | **Fixed** (session 2) — `CORSMiddleware` added in `main.py`, `allow_origins=["*"]`. Revisit origin allowlist before the 2.0 Streamlit UI ships to anything but localhost.                                                                                                                                                                                                             |
| 10  | Preprocessor CSV paths relative to cwd, not package dir                                                              | Portability                       | **Fixed** (session 2) — resolved via `os.path.dirname(os.path.abspath(__file__))` in `text_preprocessor.py`.                                                                                                                                                                                                                                                                          |
| 11  | Version string mismatch: `setup.py` = 1.0.0, `main.py` FastAPI app = 1.0.1                                           | Cosmetic                          | **Fixed** (session 2) — `setup.py` bumped to `1.0.1` to match `main.py`'s `APP_VERSION`. Still two separate literals (no shared source of truth) — acceptable for now, revisit if a third one shows up.                                                                                                                                                                               |
| 12  | No `/health` endpoint                                                                                                | Reliability / Observability       | **Fixed** (session 2) — readiness-aware, reports real `model_loaded` state, 503 when false. Caveat: per-worker state under `--workers 4`; mitigated by moving to one worker per container (see INFRA.md, TODO.md).                                                                                                                                                                    |
| 13  | Training labels are `TextBlob` polarity heuristics, not human-annotated ground truth                                 | Model quality ceiling             | Known, accepted for v1.0.0 — revisit with 2.0's HF dataset switch                                                                                                                                                                                                                                                                                                                     |
| 14  | `text_preprocessor.py` trailing-whitespace bug after emoji conversion                                                | Correctness                       | **Fixed** (session 1) — added `.strip()`. Regression test deferred to v2.0.0's test suite.                                                                                                                                                                                                                                                                                            |
| 15  | README badge says `FastAPI 0.68.1`; pinned `requirements.txt`/`dev-requirements.txt` says `fastapi==0.103.2`         | Cosmetic                          | Open — found session 2, not yet fixed.                                                                                                                                                                                                                                                                                                                                                |

## Session 2 — introduced-and-fixed-same-session

Logged for the audit trail, not because it shipped to anyone:

- Fixing #3 changed `classify_sentiment`'s return type for `return_probabilities=True` from a numpy array to a plain `list` (to match `TextProbResponse`'s declared `list[float]`). This broke `gui.py::update_radar_chart`, which called `.tolist()` on the result. Caught and fixed in the same session — `update_radar_chart` now does `np.asarray(probabilities)` on entry.
 
## Accepted for v1.0.0 (not bugs, deliberate scope)

- No auth, no rate limiting, no batching.
- Single hardcoded model version served (`0.2v`).
- No CI at all in v1.0.0 — a test workflow was drafted this session but deferred to v2.0.0 by decision.

## Deferred to 2.0.0 (breaking)

Findings #2 and #13 require the planned retrain on a fresh HF dataset with a fine-tuned transformer (DeBERTa/RoBERTa/ModernBERT candidate — see ROADMAP.md). #7 needs a decision on weight delivery (release asset / registry / manual step), not a retrain — carried into ROADMAP.md's new production-grade track rather than the model/data track.
