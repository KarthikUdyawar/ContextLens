# Decisions & Findings — v1.0.0 baseline audit

Findings from a full code review, logged here rather than silently fixed, so the baseline is honest about what v1.0.0 actually does. Numbered for reference from other docs. Status = current state, not yet remediated unless noted.

| #   | Finding                                                                                                              | Severity                          | Status                                                                                                                                                                        |
| --- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Double softmax: `forward()` softmaxes, `predict.py` softmaxes again                                                  | Correctness (predict-prob values) | Open                                                                                                                                                                          |
| 2   | `BCEWithLogitsLoss` used against softmax-normalized, mutually-exclusive output                                       | Correctness (training)            | Open — needs retrain, deferred to 2.0                                                                                                                                         |
| 3   | Text preprocessed twice per `/predict` request (once explicit, once inside `classify_sentiment`)                     | Performance                       | Open                                                                                                                                                                          |
| 4   | Missing model checkpoint fails silently ("Model not loaded", no exception, server still starts)                      | Reliability                       | Open                                                                                                                                                                          |
| 5   | `dev-requirements.txt` pins `torch==2.1.0+cu121`, breaks `pip install` off CUDA machines                             | Portability / DX                  | Open                                                                                                                                                                          |
| 6   | `BertTokenizer.from_pretrained()` reloaded on every `TextDataset` instantiation → every inference request            | Performance (latency)             | Open                                                                                                                                                                          |
| 7   | Model weights (`*.pth`) not committed, not fetched anywhere — fresh clone/Docker build cannot serve real predictions | Reliability / DX                  | **Known limitation, by design (gitignored on purpose — weights are large binaries)**. Needs an explicit fetch step (release asset / registry) before this stops being silent. |
| 8   | `Field(..., example=...)` is Pydantic v1 syntax under pinned `pydantic==2.4.2`                                       | Compat, unverified impact         | Open — needs verification                                                                                                                                                     |
| 9   | No CORS middleware                                                                                                   | Blocks browser frontend           | Open — needed for 2.0 Streamlit UI                                                                                                                                            |
| 10  | Preprocessor CSV paths relative to cwd, not package dir                                                              | Portability                       | Open                                                                                                                                                                          |
| 11  | Version string mismatch: `setup.py` = 1.0.0, `main.py` FastAPI app = 1.0.1                                           | Cosmetic                          | Open                                                                                                                                                                          |
| 12  | No `/health` endpoint                                                                                                | Ops gap                           | Open                                                                                                                                                                          |
| 13  | Training labels are `TextBlob` polarity heuristics, not human-annotated ground truth                                 | Model quality ceiling             | Known, accepted for v1.0.0 — revisit with 2.0's HF dataset switch                                                                                                             |
| 14  | `text_preprocessor.py` trailing-whitespace bug after emoji conversion                                                | Correctness                       | **Fixed** (this session) — added `.strip()`. Regression test deferred to v2.0.0's test suite.                                                                                 |

## Accepted for v1.0.0 (not bugs, deliberate scope)

- No auth, no rate limiting, no batching.
- Single hardcoded model version served (`0.2v`).
- No CI at all in v1.0.0 — a test workflow was drafted this session but deferred to v2.0.0 by decision.

## Deferred to 2.0.0 (breaking)

#1–#3, #6, #8–#12 are safe to fix without retraining and are good 2.0 early wins. #2 and #13 require the planned retrain on a fresh HF dataset with a fine-tuned transformer (DeBERTa/RoBERTa/ModernBERT candidate — see ROADMAP.md).
